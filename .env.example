# GitLab 연동 환경변수
# 실제 값은 .env 파일에 저장 (gitignore 대상)

# GitLab 호스트 URL
GITLAB_URL=https://gitlab.com

# Personal Access Token (projects, read_api, write_api 권한 필요)
GITLAB_TOKEN=your_personal_access_token_here

# 프로젝트 ID (GitLab 프로젝트 URL의 /projects/:id)
GITLAB_PROJECT_ID=123
